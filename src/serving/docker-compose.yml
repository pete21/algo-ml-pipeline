services:
  model_serving:
    hostname: model_serving
    container_name: model_serving
    build:
      context: .
      dockerfile: Dockerfile
    image: algo-ml-model-serving:latest
    ports:
      - "${MODEL_SERVE_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      MLFLOW_TRACKING_URI: ${MLFLOW_TRACKING_URI:-http://host.docker.internal:5000/}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    networks:
      - marketbroker_net

networks:
    marketbroker_net:
        name: marketbroker_net-network
        external: true
